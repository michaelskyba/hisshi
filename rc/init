#!/usr/bin/env hsh

alias:
	# $1: name
	# $2: body
	printf "%s: %s $@" $1 $2 |= cmd
	eval $cmd
	global $1

alias al alias

al p echo
al pf printf
al t test

err:
	test $1
		p $1
	-
		pf "FAIL "
		date +%s

	exit 1

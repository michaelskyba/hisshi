cd /

echo foo |= foo
echo === global $foo

myfunc:
	echo === myfunc $foo
	pwd |= foo
	echo === myfunc $foo

myfunc

# "/" overwrites global since it already existed, and wasn't named
# to force a local
echo === global $foo
